name: PoW v1 online bounded probe

on:
  workflow_dispatch:
    inputs:
      profile:
        description: Probe profile
        required: true
        default: standard
        type: choice
        options:
          - smoke
          - standard
      seeds:
        description: Deterministic seed count (1-128)
        required: true
        default: "8"
        type: string

permissions:
  contents: read

jobs:
  bounded-probe:
    name: Fail-closed online bounded probe
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      PROFILE: ${{ inputs.profile }}
      SEEDS: ${{ inputs.seeds }}

    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Compile standalone v1 research executable
        shell: bash
        run: |
          mkdir -p build
          c++ -std=c++20 -O2 -Wall -Wextra -Werror \
            contrib/pow_research_cpp/powvm_v1.cpp -o build/powvm_v1_cpp

      - name: Verify independent probe boundaries
        shell: bash
        run: |
          python3 -m unittest test.pow_research.test_bounded_probe_v1
          python3 test/pow_research/run_cpp_bounded_probe_vectors_v1.py \
            --binary build/powvm_v1_cpp

      - name: Collect bounded-probe matrix
        shell: bash
        run: |
          python3 contrib/pow_research_cpp/bounded_probe_v1.py \
            --profile "$PROFILE" \
            --seeds "$SEEDS" \
            --source-revision "$GITHUB_SHA" \
            --output build/pow-v1-online-bounded-probe.json
          python3 -m json.tool build/pow-v1-online-bounded-probe.json >/dev/null

      - name: Render bounded-probe report
        shell: bash
        run: |
          python3 contrib/pow_research_cpp/render_bounded_probe_report_v1.py \
            --matrix build/pow-v1-online-bounded-probe.json \
            --method contrib/pow_research_v1/bounded_probe_v0.json \
            --label "GitHub Ubuntu shared runner $GITHUB_RUN_ID" \
            --output build/pow-v1-online-bounded-probe.md

      - name: Preserve raw matrix and report
        uses: actions/upload-artifact@v7
        with:
          name: pow-v1-online-bounded-probe-${{ github.run_id }}
          path: |
            build/pow-v1-online-bounded-probe.json
            build/pow-v1-online-bounded-probe.md
          if-no-files-found: error
          retention-days: 14
