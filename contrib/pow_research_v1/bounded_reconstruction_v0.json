{
  "format": "soveroot-pow-v1-bounded-first-reconstruction-method-v0",
  "version": "0.1",
  "status": "NON-CONSENSUS ONE-MISS ONLINE RECONSTRUCTION METHOD",
  "question": "Can the first missing materialized scratch value and its historical machine state be reconstructed exactly inside the existing half-scratch arena?",
  "arena": {
    "budget_fraction": "1/2 of declared scratchpad",
    "allocation_count": 1,
    "fixed_state_reserve_bytes": 512,
    "write_bitmap": "one bit per logical scratch word",
    "entry_bytes": 16,
    "entry_split": "5/8 sparse replay workspace and 3/8 direct-mapped primary cache"
  },
  "reconstruction": [
    "stop before mutating state at the first missing materialized read",
    "clear the reserved replay table and execute the canonical VM from iteration zero to the missing read's iteration",
    "store every distinct replay-written word in a bounded open-addressing table and refuse if it fills",
    "require replayed registers and accumulator to equal the live pre-read machine state",
    "read the requested word from the exact replay state, commit to the value and state, retain it in the primary cache, and retry the interrupted iteration",
    "refuse without output at the next materialized miss"
  ],
  "limitations": [
    "only one materialized miss is reconstructed",
    "the replay begins at iteration zero and does not yet use recursive or checkpointed reconstruction",
    "the 512-byte reserve is logical rather than measured stack and allocator usage",
    "the smaller primary cache changes the first-miss boundary relative to the earlier probe",
    "every fixed run still refuses without a digest"
  ],
  "gate_mapping": {
    "time_memory_tradeoff": "NOT_ASSESSED",
    "reason": "one exact recovery is not a completed proof, actual process memory is unmeasured, and no throughput result exists"
  },
  "next_use": "repeat bounded reconstruction across successive misses without overflow, then require exact final outputs"
}
